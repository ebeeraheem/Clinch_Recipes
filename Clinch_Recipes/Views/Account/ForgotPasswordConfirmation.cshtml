@{
    ViewData["Title"] = "Password Reset Email Sent - CodeStash";
}

<div class="forgot-password-container">
    <div class="container">
        <div class="forgot-password-header">
            <h1><i class="fas fa-envelope-open-text text-primary me-3"></i>Password Reset Email Sent</h1>
            <p>If an account with that email exists, a password reset link has been sent.</p>
        </div>

        <div class="form-card" style="text-align:center;">
            <div class="alert alert-success" style="font-size:1.1rem;">
                <i class="fas fa-check-circle me-2" style="color:#28a745;"></i>
                Please check your email inbox and spam folder for the reset link.
            </div>
            <a href="/Account/Login" class="btn btn-primary mt-3">
                <i class="fas fa-sign-in-alt me-2"></i>Back to Login
            </a>
        </div>
    </div>
</div>

<style>
    .forgot-password-container {
        background: var(--bg-primary);
        min-height: calc(100vh - 76px);
        padding: 2rem 0;
    }

    .forgot-password-header {
        background: linear-gradient(135deg, var(--bg-secondary) 0%, var(--bg-tertiary) 100%);
        border: 1px solid var(--border-primary);
        border-radius: 8px;
        padding: 2rem;
        margin-bottom: 2rem;
        text-align: center;
    }

        .forgot-password-header h1 {
            color: var(--text-primary);
            font-size: 2.5rem;
            font-weight: 700;
            margin-bottom: 0.5rem;
        }

        .forgot-password-header p {
            color: var(--text-secondary);
            font-size: 1.1rem;
        }

    .form-card {
        background: var(--bg-tertiary);
        border: 1px solid var(--border-primary);
        border-radius: 12px;
        padding: 2.5rem;
        box-shadow: 0 8px 30px rgba(0, 0, 0, 0.3);
        max-width: 600px;
        margin: 0 auto;
    }

    .alert-success {
        background: rgba(40, 167, 69, 0.1);
        border: 1px solid #28a745;
        color: #28a745;
        border-radius: 8px;
        padding: 1.5rem;
        margin-bottom: 1.5rem;
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 0.75rem;
    }

    .btn-primary {
        background: linear-gradient(135deg, var(--accent-secondary), var(--accent-primary));
        color: white;
        padding: 1rem 2rem;
        border: none;
        border-radius: 6px;
        font-size: 1rem;
        font-weight: 600;
        cursor: pointer;
        transition: all 0.3s ease;
        text-decoration: none;
        display: inline-flex;
        align-items: center;
        gap: 0.5rem;
    }

        .btn-primary:hover {
            box-shadow: 0 8px 25px rgba(88, 166, 255, 0.3);
        }

    @@media (max-width: 768px) {
        .form-card

    {
        padding: 1.5rem;
        margin: 0 1rem;
    }

    .forgot-password-header {
        padding: 1.5rem;
    }

        .forgot-password-header h1 {
            font-size: 2rem;
        }

    }
</style>
